# greentea

This repo is for general practice of Git commands
git add --all
git commit -m "Hello"
git push origin prod
git checkout prod
git branch
git diff
git status
git stash
git checkout -b feature/task

git pull origin dev
